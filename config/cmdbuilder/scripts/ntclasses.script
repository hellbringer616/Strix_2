#Runs the NT-RPG command /classes
#made as a work around for when you need the player to run the command
#when only the server can
arguments=[]
executors=[
    {
        command="classes"
        source="Player"
    }
]
metadata{
    aliases=["ntclasses"]
}
