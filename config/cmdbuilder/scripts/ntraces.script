#Runs the NT-RPG command /races
#made as a work around for when you need the player to run the command
#when only the server can
arguments=[]
executors=[
    {
        command="races"
        source="Player"
    }
]
metadata{
    aliases=["ntraces"]
}
