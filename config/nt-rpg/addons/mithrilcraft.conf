Skills: [
    {
        Id: "craft:mithrilbar"
        Name: SmeltMithrilBar
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Lore: [
            "Learn to Smelt Mithril."
        ]
        Description: "Smelt Mithril Bars"
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 5, 2) && Itemizer.hasItemizerItem(player, 4, 4)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 29 metallurgy metallurgy");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[5, 2], [4, 4]], [24, 1]);
            """
    }
    {
        Id: "craft:mithrilpick"
        Name: ForgeMithrilPick
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Lore: [
            "Learn the power to temper Mithril into a Pick"
        ]
        Description: "Forge Mithril Pick"
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 23, 2)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 70 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[24, 2]], [1003, 1]);
            """
    }
    {
        Id: "craft:mithrildagger"
        Name: ForgeMithrilDagger
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Lore: [
            "Learn the power to temper Mithril into a Dagger"
        ]
        Description: "Forge Mithril Dagger"
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 24, 2)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 70 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[24, 2]], [1303, 1]);
            """
    }
    {
        Id: "craft:mithrilmace"
        Name: ForgeMithrilMace
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Lore: [
            "Learn the power to temper Mithril into a Mace"
        ]
        Description: "Forge Mithril Mace"
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 24, 3)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 104.5 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[24, 3]], [1203, 1]);
            """
    }
    {
        Id: "craft:mithrillongsword"
        Name: ForgeMithrilLongsword
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Lore: [
            "Learn the power to temper Mithril into a Longsword"
        ]
        Description: "Forge Mithril Longsword"
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 24, 4)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 139.5 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[24, 4]], [1103, 1]);
            """
    }
    {
        Id: "craft:mithrilhatchet"
        Name: ForgeMithrilHatchet
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Lore: [
            "Learn the power to temper Mithril into a Axe"
        ]
        Description: "Forge Mithril Axe"
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 24, 2)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 70 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[24, 2]], [1023, 1]);
            """
    }
    {
        Id: "craft:mithrilsabatons"
        Name: ForgeMithrilSabatons
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Lore: [
            "Forge Mithril into a protective Mithril Sabatons"
        ]
        Description: "Forge Mithril Sabatons"
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 24, 4)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 139.5 armorer armorer");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[24, 4]], [9130, 1]);
            """
    }
    {
        Id: "craft:mithrilhelm"
        Name: ForgeMithrilHelm
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Lore: [
            "Forge Mithril into a protective Mithril Helm"
        ]
        Description: "Forge Mithril Helm"
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 24, 5)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 174 armorer armorer");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[24, 5]], [9131, 1]);
            """
    }
    {
        Id: "craft:mithrillegs"
        Name: ForgeMithrilLegs
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Lore: [
            "Forge Mithril into a protective Mithril Legs"
        ]
        Description: "Forge Mithril Legs"
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 24, 7)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 244 armorer armorer");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[24, 7]], [9132, 1]);
            """
    }
    {
        Id: "craft:mithrilplate"
        Name: ForgeMithrilPlate
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Lore: [
            "Forge Mithril into a protective Mithril Plate"
        ]
        Description: "Forge Mithril Plate"
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 24, 8)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 278.5 armorer armorer");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[24, 8]], [9133, 1]);
            """
    }
]
